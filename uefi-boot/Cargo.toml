[package]
name = "uefi-boot"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "0.39", features = ["alloc", "global_allocator", "logger", "panic_handler"] }
log = "0.4"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "s"
