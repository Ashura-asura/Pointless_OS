echo "=== Track2 battery begin ==="
uname -a
echo "SHELL=$0 PS1=[$PS1]"
echo "--- job control test (expect 'jobs' to list the background sleep) ---"
sleep 1 & jobs
echo "--- python3 (battery item 2) ---"
python3 --version 2>&1 || echo "python3: MISSING"
echo "--- git (battery item 3) ---"
git --version 2>&1 || echo "git: MISSING"
echo "--- vim / nano (battery item 4) ---"
vim --version 2>&1 | head -1 || echo "vim: MISSING"
nano --version 2>&1 | head -1 || echo "nano: MISSING"
echo "--- gcc / make (battery item 5) ---"
gcc --version 2>&1 | head -1 || echo "gcc: MISSING"
make --version 2>&1 | head -1 || echo "make: MISSING"
echo "--- /proc (needed by ps/meminfo/top) ---"
ls /proc 2>&1 | head -5 || echo "/proc: MISSING"
cat /proc/meminfo 2>&1 | head -3 || echo "/proc/meminfo: MISSING"
echo "--- networking (e1000e path target) ---"
ip a 2>&1 | head -5 || echo "ip: MISSING"
ifconfig 2>&1 | head -5 || echo "ifconfig: MISSING"
echo "--- /dev (device surface) ---"
ls /dev 2>&1 | head -20
echo "=== Track2 battery end ==="
