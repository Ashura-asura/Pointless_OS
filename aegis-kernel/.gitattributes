# Phase AE committed fuzz corpus: treat as opaque binary so line-ending
# normalization never mutates seeds (deterministic replay must match the
# bytes generated by fuzz_corpus.rs).
fuzz-corpus/** binary
