[package]
name = "security-audit"
version = "0.1.0"
edition = "2021"

[dependencies]
capability-core = { path = "../capability-core" }
capability-audit = { path = "../capability-audit" }
grants = { path = "../grants" }
