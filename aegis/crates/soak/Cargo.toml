[package]
name = "soak"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
license = "MIT OR Apache-2.0"

[dependencies]
capability-core = { path = "../capability-core" }
supervision-tree = { path = "../supervision-tree" }
resources = { path = "../resources" }
orchestration = { path = "../orchestration" }
fleet = { path = "../fleet" }
capability-audit = { path = "../capability-audit" }
